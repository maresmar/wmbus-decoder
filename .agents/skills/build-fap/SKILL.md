---
name: build-fap
description: Build an application into a Flipper-compatible fap file.
---

Build the app from repo root:

```bash
ufbt
```

Expected artifact:

```text
dist/wmbus_decoder.fap
```

